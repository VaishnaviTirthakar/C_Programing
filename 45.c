#include <stdio.h>
int main() {
    int num = 25;
    if (num >= 10 && num <= 50)
        printf("Number is between 10 and 50\n");
    else
        printf("Number is outside the range\n");
    return 0;
}
