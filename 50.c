#include <stdio.h>
int main() {
    int x = 5;
    if (x > 0 && x < 10)
        printf("x is a single-digit positive number\n");
    else
        printf("x is not a single-digit positive number\n");
    return 0;
}
